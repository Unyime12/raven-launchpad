[package]
name = "raven-launchpad"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
token = { path = "../token" }

[dev-dependencies]
soroban-sdk = { version = "21.7.7", features = ["testutils"] }
ed25519-dalek = "=2.1.1"